{% extends 'base.html.twig' %}

{% block title %}
	{{ episode.title }}
{% endblock %}

{% block body %}
	<div class="media">
		<div class="media-body">
			<h1 class="mt-0">Série : 
				{{program.title}}</h1>
		</div>
		<div>
			<h2 class="mt-0">Saison : 
				{{season.number}}</h2>
		</div>
		<div>
			<p>Episode :
				{{episode.number}}</p>
			<p>{{episode.title}}</p>
		</div>
        <p>{{episode.synopsis}}</p>
	</div>
<a href="{{ path('program_index') }}">
	Retour à la liste des programmes
</a>{% endblock %}
